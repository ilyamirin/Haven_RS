# Core runtime dependencies for the multi-agent recommender
rich>=13.7.0

# LangGraph for workflow orchestration
langgraph>=0.2.30

# (Optional) LangChain core used by LangGraph internals in some setups
langchain>=0.2.10
langchain-community>=0.2.10

# Transformers-based LLM
transformers>=4.43.0
# Explicit HF utils for downloading and caching models
huggingface-hub>=0.23.0
# Torch with platform markers
torch>=2.1; platform_system != "Windows"
torch>=2.2; platform_system == "Windows"
accelerate>=0.30.0
safetensors>=0.4.2
sentencepiece>=0.1.99
# Data handling
pandas>=2.0.0
# Chat template rendering in Transformers can require Jinja2
jinja2>=3.1.0
