# Core runtime dependencies for the multi-agent recommender
rich>=13.7.0

# LangGraph for workflow orchestration
langgraph>=0.2.30

# LangChain core and community split (v0.2+)
langchain>=0.2.10
langchain-community>=0.2.10

# Ollama integration (choose either langchain-ollama or community fallback in code)
langchain-ollama>=0.1.0

# Python client for a running local Ollama server
ollama>=0.3.0
